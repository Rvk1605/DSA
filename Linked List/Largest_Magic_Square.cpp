#include<bits/stdc++.h>
using namespace std;

int largestMagicSquare(vector<vector<int>> &grid){
int length=0;
for(int i=0;i<grid.size();i++){
    for(int j=0;j<grid[0].size();j++){

    }
}

}

int main(){
vector<vector<int>> grid;
for(int i=0;i<grid.size();i++){
        vector<int> g;
    for(int j=0;i<grid[o].size();j++){
        int x;
        cin>>x;
        g.push_back(x);
    }
    grid.push_back(g);
}
 cout<<largestMagicSquare(grid)<<endl;
}
